async function loadUsers(){ const res = await fetch('/api/users/list'); const data = await res.json(); const list = document.getElementById('list'); list.innerHTML = ''; (data.list || []).forEach(u=>{ const div = document.createElement('div'); div.innerHTML = '<b>'+u.email+'</b> â€” <span class=\"badge\">'+u.role+'</span>'; list.appendChild(div); }); } async function addUser(){ const body = { email: document.getElementById('email').value, role: document.getElementById('role').value }; await fetch('/api/users/add', { method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify(body) }); loadUsers(); } loadUsers(); 
